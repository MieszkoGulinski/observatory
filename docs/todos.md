# TODO list

Add skeleton of the Arduino code

Add calculation of LHA given RA, UTC time and longitude

Add calculation of RA for zero LHA to point the telescope at zenith, to take flat frames

Add validation that LHA must not exceed +/- 90 degrees, as the telescope cannot point through the mount

Red on black styling
